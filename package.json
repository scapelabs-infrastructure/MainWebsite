{
  "name": "leap-app",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "dependencies": {
    "express": "^5.2.1",
    "nodemailer": "^8.0.1"
  }
}
