{
  "name": "leap-app",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node server/index.cjs"
  },
  "dependencies": {
    "express": "^5.2.1",
    "nodemailer": "^8.0.1"
  }
}
